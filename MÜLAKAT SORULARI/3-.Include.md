### 📌 Ne işe yarar:

Bu, **Eager Loading** yöntemidir.

EF Core varsayılan olarak sadece `Hotels` tablosunu çeker.

Ama her otelin odalarını (`Rooms`) da birlikte getirmek istiyorsan, `Include` kullanırsın.

```csharp
.Include(h => h.Rooms)

```

Bu şu SQL’e dönüşür:

```sql
SELECT * FROM Hotels
LEFT JOIN Rooms ON Rooms.HotelId = Hotels.Id

```

Yani EF Core aynı anda iki tabloyu birleştirip getirir.

---

### 💡 Neden kullanıyoruz:

- Tek sorguda hem otel hem odalar gelir.
- “N+1 sorgu problemi” (her otel için ayrı sorgu) önlenir.
- Controller’a dönecek veride `Rooms` listesi **dolu** olur.

🧠 Örnek:

```csharp
foreach (var hotel in hotels)
{
    Console.WriteLine($"{hotel.Name} otelinde {hotel.Rooms.Count} oda var.");
}

```

`Include` olmasaydı `hotel.Rooms` boş olurdu (veya lazy loading gerekirdi).